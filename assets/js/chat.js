document.addEventListener('DOMContentLoaded',()=>{setTimeout(initChat,0)});
function initChat(){const box=document.querySelector('.page'); if(!box)return; box.innerHTML=`<section class="card chat-layout"><div class="chat-head"><div class="chat-title"><div class="chat-icon">💬</div><div><h2>Общий чат</h2><p class="muted">локальный чат · стикеры · эмодзи · звук</p></div></div><button class="btn" onclick="clearChat()">Очистить</button></div><div class="messages" id="messages"></div><div class="composer"><button class="iconbtn" onclick="togglePicker('emojiPicker')">☺</button><input class="input" id="msgInput" placeholder="Сообщение..." autocomplete="off"><button class="iconbtn bell" onclick="testNotify()">🔔</button><button class="iconbtn" onclick="sendMessage()">➤</button><div class="picker" id="emojiPicker"><h3>Смайлики</h3><div class="emoji-grid">${emojis.map(e=>`<button onclick="insertText('${e}')">${e}</button>`).join('')}</div><h3>Стикеры</h3><div class="emoji-grid">${stickers.map(e=>`<button class="sticker" onclick="sendSticker('${e}')">${e}</button>`).join('')}</div><h3>Свой стикер / GIF / фото</h3><input class="input" type="file" accept="image/*,.gif" onchange="customSticker(event)"></div></div></section>`;renderMessages();document.getElementById('msgInput').addEventListener('keydown',e=>{if(e.key==='Enter')sendMessage()});}
function msgs(){return get('chat',[{user:'system',text:'Добро пожаловать в общий чат Дачи 2004',time:new Date().toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'}),system:true}])}
function saveMsgs(v){set('chat',v)}
function renderMessages(){let m=msgs(), el=document.getElementById('messages'); if(!el)return; el.innerHTML=m.map(x=>`<div class="msg ${x.user==='sweetsvt'?'me':''}"><div class="meta">${x.user} ${x.time||''}</div>${x.img?`<img src="${x.img}" style="max-width:220px;border-radius:18px;display:block">`:x.text}</div>`).join(''); requestAnimationFrame(()=>el.scrollTop=el.scrollHeight)}
function sendMessage(){let inp=document.getElementById('msgInput'); let text=inp.value.trim(); if(!text)return; let arr=msgs(); arr.push({user:me().name||'sweetsvt',text,time:new Date().toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'})}); saveMsgs(arr); inp.value=''; renderMessages(); playSound();}
function insertText(t){let inp=document.getElementById('msgInput'); inp.value+=t; inp.focus()}
function sendSticker(s){let arr=msgs(); arr.push({user:me().name||'sweetsvt',text:`<span class="sticker">${s}</span>`,time:new Date().toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'})}); saveMsgs(arr); renderMessages(); playSound()}
function customSticker(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{let arr=msgs();arr.push({user:me().name||'sweetsvt',img:r.result,time:new Date().toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'})});saveMsgs(arr);renderMessages();playSound()};r.readAsDataURL(f)}
function togglePicker(id){document.getElementById(id).classList.toggle('open')}
function clearChat(){if(confirm('Очистить чат?')){localStorage.removeItem(STORE+'chat');renderMessages()}}
function testNotify(){if('Notification'in window){Notification.requestPermission().then(p=>{if(p==='granted')new Notification('Дача 2004',{body:'Уведомления работают'});else toast('Разреши уведомления в браузере')})}playSound()}
